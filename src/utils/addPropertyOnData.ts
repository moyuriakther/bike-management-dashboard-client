// export const addPropertyOnData = (data: any) => {
//   const newKey = "key";
//   const newData = data?.map((item: any) => ({
//     ...item,
//     [newKey]: generateUniqueId(),
//   }));
//   return newData;
// };

// function generateUniqueId() {
//   return (Math.random() + 1).toString(36).substring(7);
// }
